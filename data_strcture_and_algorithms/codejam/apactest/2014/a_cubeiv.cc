#include <fstream>
#include <string>
#include <vector>

using std::basic_ifstream;
using std::string;
using std::vector;

int main() {
  int total_cases;
  vector<vector<int>> cur_case{{3, 4}, {1, 2}};

  int r;
  int max_steps;
  int d;
  for (int i = 0; i < cur_case.size(); ++i) {
    for (int j = 0; j < cur_case.size(); ++j) {
      while(true){

      }
    }
  }

  return 0;
}
