//
// Created by chaomai on 5/19/15.
//

#include "naive.h"

void search(char *pat, char *txt) {
  size_t pat_len = strlen(pat);
  size_t txt_len = strlen(txt);

  size_t i;
  for (i = 0; i < txt_len; ++i) {

  }
}
